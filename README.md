# Deep Keypharse Generation with one-to-many

## Dataset
For a new dataset, you should create a new directory and place it in the ./data directory. 
It should contains three pairs of source-target files, as follows.
train_src.txt, train_trg.txt, valid_src.txt, valid_trg.txt, test_src.txt

### Formats
All the text should be tokenized and each token should be separated by a space character.
All digits should be replaced by a '<digit>' tokens.
#### Source file


## Preprocess data

